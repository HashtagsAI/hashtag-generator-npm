export declare type HashtagsResponse = {
    tags: string[];
};

export declare type Category = {
    name: string;
    id: number;
};

export declare type CategoriesResponse = {
    categories: Category[];
};

export declare type CountResponse = {
    count: number;
}

export declare type CountriesResponse = {
    countries: string[];
};